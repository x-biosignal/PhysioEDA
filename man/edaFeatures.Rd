% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda-features.R
\name{edaFeatures}
\alias{edaFeatures}
\title{Extract EDA Features per Channel}
\usage{
edaFeatures(
  x,
  peaks = NULL,
  window = NULL,
  assay_name = NULL,
  ns_scr_amplitude_min = 0.05
)
}
\arguments{
\item{x}{A PhysioExperiment object containing EDA data.}

\item{peaks}{Optional pre-computed peaks data.frame from
\code{\link{edaPeaks}}. If NULL, peaks are computed automatically
using the gradient method.}

\item{window}{Optional numeric vector of length 2 giving the time window
in seconds as \code{c(start_sec, end_sec)} to restrict analysis.}

\item{assay_name}{Name of the input assay for peak detection. If NULL,
uses \code{defaultAssay(x)}.}

\item{ns_scr_amplitude_min}{Minimum SCR amplitude (in uS) to count a peak as
a non-specific SCR (default 0.05, the standard threshold).}
}
\value{
A \code{data.frame} with one row per channel and the following
columns:
\describe{
\item{channel}{Character channel label.}
\item{scr_count}{Integer number of detected SCR peaks.}
\item{scr_rate_per_min}{Numeric SCR count per minute.}
\item{mean_amplitude}{Numeric mean SCR peak amplitude in microsiemens
(0 if no peaks detected).}
\item{mean_scl}{Numeric mean tonic skin conductance level.}
\item{scl_sd}{Numeric standard deviation of the tonic signal.}
\item{auc_phasic}{Numeric area under the phasic curve (positive values
only, in \code{uS*s}), or \code{NA} if no phasic assay exists.}
\item{ns_scr_count}{Integer count of non-specific SCRs (amplitude >=
\code{ns_scr_amplitude_min}).}
\item{ns_scr_freq}{Numeric non-specific SCR frequency (per minute):
\code{ns_scr_count / duration_min}.}
\item{cda_nscr}{Integer number of significant SCRs in the CDA/DDA driver,
or \code{NA} if no driver assay exists.}
\item{cda_ampsum}{Numeric sum of the CDA/DDA driver SCR amplitudes (uS),
or \code{NA}.}
\item{cda_iscr}{Numeric integrated SCR: time-integral of the phasic
driver (\code{uS*s}), or \code{NA}.}
}
}
\description{
Computes summary features from electrodermal activity data, including
skin conductance response (SCR) statistics and skin conductance level
(SCL) measures. Requires tonic/phasic decomposition to have been
performed (via \code{\link{edaDecompose}}).
}
\references{
Boucsein, W. (2012). \emph{Electrodermal Activity}. 2nd ed. Springer.
\doi{10.1007/978-1-4614-1126-0}

Benedek, M., & Kaernbach, C. (2010). "A continuous measure of phasic
electrodermal activity." \emph{Journal of Neuroscience Methods}, 190(1),
80-91. \doi{10.1016/j.jneumeth.2010.04.028}
}
\seealso{
\code{\link{edaDecompose}} for tonic/phasic decomposition (run first),
\code{\link{edaPeaks}} for SCR peak detection,
\code{\link{edaErscr}} for event-related SCR analysis
}
