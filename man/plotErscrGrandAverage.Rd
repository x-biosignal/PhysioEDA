% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda-plot.R
\name{plotErscrGrandAverage}
\alias{plotErscrGrandAverage}
\title{Grand-average event-related skin conductance response (ER-SCR)}
\usage{
plotErscrGrandAverage(
  x,
  event_types = NULL,
  window = c(-1, 6),
  baseline = c(-1, 0),
  channel = 1,
  assay_name = NULL,
  ci = c("sem", "none"),
  ci_level = 0.95,
  colors = NULL,
  plot = TRUE,
  ...
)
}
\arguments{
\item{x}{A PhysioExperiment object with a skin conductance assay and events.}

\item{event_types}{Character vector of event types to average and overlay;
\code{NULL} (default) uses every event type present.}

\item{window}{Numeric \code{c(pre, post)} epoch limits in seconds relative to the
event onset (default \code{c(-1, 6)}).}

\item{baseline}{Numeric \code{c(start, end)} pre-event baseline interval in seconds
subtracted from each epoch, or \code{NULL} for no baseline correction
(default \code{c(-1, 0)}).}

\item{channel}{Channel index to average (default 1).}

\item{assay_name}{Input assay (default: \code{defaultAssay(x)}).}

\item{ci}{Confidence band: \code{"sem"} (mean +/- z * SEM, default) or \code{"none"}.}

\item{ci_level}{Confidence level for the SEM band (default 0.95).}

\item{colors}{Optional per-condition colours (recycled).}

\item{plot}{Draw the plot (default \code{TRUE}); set \code{FALSE} to only compute.}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
Invisibly, an \code{erscr_grand_average} object with \code{grand_average} (a
tidy data frame of \code{type}, \code{time}, \code{mean}, \code{sd}, \code{sem}, \code{ci_lower},
\code{ci_upper}, \code{n_trials}), the per-condition baseline-corrected \code{epochs}
matrices, and the \code{time} axis.
}
\description{
Computes and plots the grand-average phasic skin-conductance waveform
time-locked to stimulus events, separately per event type (condition), with a
confidence-interval band. Each event's epoch over \code{window} is baseline
corrected against the pre-event \code{baseline} interval, epochs are averaged
within a condition, and the band is the standard error of the mean scaled to
\code{ci_level} (Boucsein 2012; Bach event-related SCR averaging). The epoching
reuses the event onsets from \code{\link[PhysioCore:getEvents]{PhysioCore::getEvents()}} on the same sample grid as
\code{\link[=edaErscr]{edaErscr()}}.
}
\examples{
x <- make_eda_with_scr()
ga <- plotErscrGrandAverage(x, plot = FALSE)
head(ga$grand_average)
}
\references{
Boucsein, W. (2012). Electrodermal Activity. 2nd ed. Springer.
}
\seealso{
\code{\link[=edaErscr]{edaErscr()}}
}
