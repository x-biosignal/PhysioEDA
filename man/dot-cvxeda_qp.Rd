% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda-cvxeda.R
\name{.cvxeda_qp}
\alias{.cvxeda_qp}
\title{Exact cvxEDA Decomposition for a Single Channel (Greco QP)}
\usage{
.cvxeda_qp(
  signal,
  sr,
  tau1 = 0.75,
  tau2 = 2,
  alpha = 0.01,
  gamma = 0.1,
  delta_knot = 10,
  qp_sr = 4,
  max_n = 2000
)
}
\arguments{
\item{signal}{Numeric vector of EDA signal values.}

\item{sr}{Sampling rate in Hz.}

\item{tau1}{SCR rise time constant in seconds (default: 0.75).}

\item{tau2}{SCR decay time constant in seconds (default: 2.0).}

\item{alpha}{L1 sparsity penalty on the driver (default: 0.01).}

\item{gamma}{L2 smoothness weight on the tonic (default: 0.1).}

\item{delta_knot}{Tonic spline knot spacing in seconds (default: 10).}

\item{qp_sr}{Target rate (Hz) for the QP solve; the signal is decimated
towards this rate (default: 4).}

\item{max_n}{Maximum number of samples in the QP; further decimation is
applied above this (default: 2000).}
}
\value{
A list with \code{tonic}, \code{phasic}, and \code{driver} on the
native grid, plus \code{driver_qp} / \code{t_qp} / \code{sr_qp} at the QP
rate, the QP \code{objective} and \code{obj_init}, \code{converged}, and the
\code{engine} ("qp" or "wiener_fallback") and \code{solver} used.
}
\description{
Full-signal wrapper around \code{\link{.cvxeda_qp_core}}: it decimates the
signal to a tractable rate for the dense quadratic program (electrodermal
activity is slow, so a few Hz suffices), solves the exact cvxEDA QP, and
interpolates the tonic / phasic / driver components back to the native grid.
If \pkg{quadprog} is unavailable, or the solve fails, it degrades gracefully
to the Wiener approximation (\code{\link{.cvxeda_decompose}}).
}
\references{
Greco, A., et al. (2016). "cvxEDA: A convex optimization approach to
electrodermal activity processing." \emph{IEEE Transactions on Biomedical
Engineering}, 63(4), 797-804. \doi{10.1109/TBME.2015.2474131}
}
\seealso{
\code{\link{.cvxeda_decompose}} for the fast Wiener approximation,
\code{\link{edaDecompose}} for the user-facing wrapper.
}
\keyword{internal}
