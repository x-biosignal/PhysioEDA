% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda-sympathetic.R
\name{edaTVSymp}
\alias{edaTVSymp}
\title{Time-varying electrodermal sympathetic index (TVSymp)}
\usage{
edaTVSymp(
  x,
  band = c(0.08, 0.24),
  assay_name = NULL,
  resample_hz = 2,
  n_components = 2,
  lp_hz = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with an electrodermal assay.}

\item{band}{Numeric \code{c(low, high)} sympathetic band in Hz (default
\code{c(0.08, 0.24)}).}

\item{assay_name}{Input assay (default: \code{defaultAssay(x)}).}

\item{resample_hz}{Rate (Hz) the signal is downsampled to (default 2; \code{NULL}
keeps the native rate).}

\item{n_components}{Number of dominant band components to demodulate
(default 2).}

\item{lp_hz}{Low-pass cutoff (Hz) of the demodulation envelope (default 0.5
of the band width).}
}
\value{
A data frame with one row per time sample per channel: \code{channel},
\code{time_sec}, \code{tvsymp} (combined instantaneous amplitude) and \code{tvsymp_norm}
(unit-variance normalised). The dominant component frequencies are attached
as the \code{"components"} attribute.
}
\description{
A time-resolved sympathetic-tone index built by complex demodulation of the
dominant components of the electrodermal signal in the sympathetic band
(Posada-Quintero et al. 2016). The two strongest spectral components in the
band are demodulated to their instantaneous amplitudes, combined, and
normalised to a reproducible trajectory that rises when sympathetic
oscillations appear.
}
\examples{
pe <- edaSimulate(n_time = 1200, sr = 4, scr_count = 6, seed = 1)
tv <- edaTVSymp(pe)
head(tv)
}
\references{
Posada-Quintero, H.F. et al. (2016). Time-varying analysis of
electrodermal activity during exercise. PLoS ONE 11(8):e0161553.
}
\seealso{
\code{\link[=edaSympatheticIndex]{edaSympatheticIndex()}}
}
