% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda-sympathetic.R
\name{edaSympatheticIndex}
\alias{edaSympatheticIndex}
\title{Electrodermal sympathetic index (EDASymp)}
\usage{
edaSympatheticIndex(
  x,
  band = c(0.045, 0.25),
  assay_name = NULL,
  resample_hz = 2,
  seg_sec = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with an electrodermal assay.}

\item{band}{Numeric \code{c(low, high)} sympathetic band in Hz (default
\code{c(0.045, 0.25)}).}

\item{assay_name}{Input assay (default: \code{defaultAssay(x)}).}

\item{resample_hz}{Rate (Hz) the signal is downsampled to before spectral
estimation (default 2; \code{NULL} keeps the native rate). Ignored if it is not
below the native sampling rate.}

\item{seg_sec}{Welch segment length in seconds (\code{NULL} = one whole-signal
Hann window).}
}
\value{
A data frame with one row per channel: \code{channel}, \code{edasymp}
(absolute band power), \code{edasymp_norm} (band power / total power, in
\verb{[0, 1]}, or \code{NA} for a constant / zero-power channel), \code{total_power}, and
the \code{band_low}/\code{band_high} used.
}
\description{
Spectral power of the electrodermal signal in the sympathetic band
(0.045-0.25 Hz by default), following Posada-Quintero et al. (2016). The
signal is optionally downsampled, its Welch power spectral density is
estimated, and the band power is reported both in absolute units and
normalised by the total spectral power.
}
\examples{
pe <- edaSimulate(n_time = 1200, sr = 4, scr_count = 6, seed = 1)
edaSympatheticIndex(pe)
}
\references{
Posada-Quintero, H.F. et al. (2016). Power spectral density
analysis of electrodermal activity for sympathetic function assessment.
Ann Biomed Eng 44(10):3124-3135.
}
\seealso{
\code{\link[=edaTVSymp]{edaTVSymp()}}
}
